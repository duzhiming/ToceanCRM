<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.student.dao.CoreUserDao">
    
	<sql id="coreUserColumns">
		a.userid AS "userid",
		a.name AS "name",
		a.loginid AS "loginid",
		a.password AS "password",
		a.admin AS "admin",
		a.usertype AS "usertype",
		a.deptid AS "deptid"
	</sql>
	
	<sql id="coreUserJoins">
	</sql>
    
	<select id="get" resultType="CoreUser">
		SELECT 
			<include refid="coreUserColumns"/>
		FROM core_user a
		<include refid="coreUserJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CoreUser">
		SELECT 
			<include refid="coreUserColumns"/>
		FROM core_user a
		<include refid="coreUserJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="loginid != null and loginid != ''">
				AND a.loginid = #{loginid}
			</if>
			<if test="password != null and password != ''">
				AND a.password = #{password}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CoreUser">
		SELECT 
			<include refid="coreUserColumns"/>
		FROM core_user a
		<include refid="coreUserJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO core_user(
			userid,
			name,
			loginid,
			password,
			admin,
			usertype,
			deptid
		) VALUES (
			#{userid},
			#{name},
			#{loginid},
			#{password},
			#{admin},
			#{usertype},
			#{deptid}
		)
	</insert>
	
	<update id="update">
		UPDATE core_user SET 	
			userid = #{userid},
			name = #{name},
			loginid = #{loginid},
			password = #{password},
			admin = #{admin},
			usertype = #{usertype},
			deptid = #{deptid}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM core_user
		WHERE id = #{id}
	</update>
	
</mapper>