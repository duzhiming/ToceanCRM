<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.student.dao.SysClassesDao">
    
	<sql id="sysClassesColumns">
		a.id AS "id",
		a.name AS "name",
		a.opendate AS "opendate",
		a.closedate AS "closedate",
		a.monitor AS "monitor",
		a.studentamount AS "studentamount",
		a.other AS "other",
		a.type_id AS "typeId",
		a.classroom_id AS "classroomId",
		a.teacher_id AS "teacherId",
		a.pm_id AS "pmId",
		a.subarea_id AS "subareaId",
		a.qq AS "qq"
	</sql>
	
	<sql id="sysClassesJoins">
	</sql>
    
	<select id="get" resultType="SysClasses">
		SELECT 
			<include refid="sysClassesColumns"/>
		FROM sys_classes a
		<include refid="sysClassesJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SysClasses">
		SELECT 
			<include refid="sysClassesColumns"/>
		FROM sys_classes a
		<include refid="sysClassesJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SysClasses">
		SELECT 
			<include refid="sysClassesColumns"/>
		FROM sys_classes a
		<include refid="sysClassesJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO sys_classes(
			id,
			name,
			opendate,
			closedate,
			monitor,
			studentamount,
			other,
			type_id,
			classroom_id,
			teacher_id,
			pm_id,
			subarea_id,
			qq
		) VALUES (
			#{id},
			#{name},
			#{opendate},
			#{closedate},
			#{monitor},
			#{studentamount},
			#{other},
			#{typeId},
			#{classroomId},
			#{teacherId},
			#{pmId},
			#{subareaId},
			#{qq}
		)
	</insert>
	
	<update id="update">
		UPDATE sys_classes SET 	
			name = #{name},
			opendate = #{opendate},
			closedate = #{closedate},
			monitor = #{monitor},
			studentamount = #{studentamount},
			other = #{other},
			type_id = #{typeId},
			classroom_id = #{classroomId},
			teacher_id = #{teacherId},
			pm_id = #{pmId},
			subarea_id = #{subareaId},
			qq = #{qq}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM sys_classes
		WHERE id = #{id}
	</update>
	
</mapper>