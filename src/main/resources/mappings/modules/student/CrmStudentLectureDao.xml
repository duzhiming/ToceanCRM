<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.student.dao.CrmStudentLectureDao">
    
	<sql id="crmStudentLectureColumns">
		a.id AS "id",
		a.class_id AS "classes.id",
		a.student_id AS "studentes.id",
		a.createrid AS "createrid",
		a.content AS "content",
		a.date AS "date",
		a.evaluation AS "evaluation",
		a.score AS "score",
		a.advice AS "advice",
		a.pm_id AS "pmId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="crmStudentLectureJoins">
	</sql>
    
	<select id="get" resultType="CrmStudentLecture">
		SELECT 
			<include refid="crmStudentLectureColumns"/>
		FROM crm_student_lecture a
		<include refid="crmStudentLectureJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CrmStudentLecture">
		SELECT 
			<include refid="crmStudentLectureColumns"/>
		FROM crm_student_lecture a
		<include refid="crmStudentLectureJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<!--<if test="studentId != null and studentId != ''">-->
				<!--AND a.student_id = #{studentes.id}-->
			<!--</if>-->
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CrmStudentLecture">
		SELECT 
			<include refid="crmStudentLectureColumns"/>
		FROM crm_student_lecture a
		<include refid="crmStudentLectureJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO crm_student_lecture(
			id,
			class_id,
			student_id,
			createrid,
			content,
			date,
			evaluation,
			score,
			advice,
			pm_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{classes.id},
			#{studentes.id},
			#{createrid},
			#{content},
			#{date},
			#{evaluation},
			#{score},
			#{advice},
			#{pmId},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE crm_student_lecture SET 	
			class_id = #{classes.id},
			student_id = #{studentes.id},
			createrid = #{createrid},
			content = #{content},
			date = #{date},
			evaluation = #{evaluation},
			score = #{score},
			advice = #{advice},
			pm_id = #{pmId},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE crm_student_lecture SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>